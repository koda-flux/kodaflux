{
  "name": "@repo/infrastructure",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "init": "terraform init",
    "plan": "terraform plan",
    "apply": "terraform apply",
    "lint": "tflint --init && tflint --minimum-failure-severity=error -f compact",
    "lint:fix": "tflint --init && tflint --minimum-failure-severity=error --fix && terraform fmt"
  }
}
