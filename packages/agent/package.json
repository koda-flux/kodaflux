{
	"name": "agent",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"dev": "uv run gradient agent run",
		"test": "uv run pytest",
		"lint": "uv run ruff check",
		"lint:fix": "uv run ruff check --fix"
	}
}
