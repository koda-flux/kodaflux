{
	"name": "agent",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"dev": "gradient agent run",
		"test": "pytest",
		"lint": "uv run ruff check",
		"lint:fix": "uv run ruff check --fix",
		"install": "uv sync"
	}
}
