{
	"name": "backend",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"dev": "fastapi dev app/main.py",
		"test": "pytest",
		"lint": "uv run ruff check",
		"lint:fix": "uv run ruff check --fix",
		"install": "uv sync"
	}
}
